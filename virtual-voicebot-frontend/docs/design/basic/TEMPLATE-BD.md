# BD-{連番}_{slug}

> {基本設計の1行サマリ}

| 項目 | 値 |
|------|-----|
| ID | BD-{連番} |
| ステータス | Draft / Review / Approved / Implemented |
| 作成日 | YYYY-MM-DD |
| 関連Issue | #{issue番号} |
| 対応RD | RD-{連番} |
| 対応IT | IT-{連番} |

---

## 1. 概要

### 1.1 目的

{この基本設計で定義する内容の目的}

### 1.2 スコープ

- {対象となるページ・コンポーネント}

---

## 2. システム構成

### 2.1 全体構成図

```
{システム構成図（テキストベース）}
```

### 2.2 コンポーネント一覧

| コンポーネント | 責務 | 技術選定 |
|--------------|------|---------|
| {名前} | {責務} | {技術} |

---

## 3. 画面構成

### 3.1 レイアウト構造

```
{レイアウト図（テキストベース）}
```

### 3.2 ページ一覧

| パス | ページ名 | コンポーネント | 説明 |
|------|---------|--------------|------|
| {パス} | {名前} | {コンポーネント} | {説明} |

---

## 4. コンポーネント構成

### 4.1 コンポーネントツリー

```
app/
├── layout.tsx
│   └── {LayoutComponent}
└── {page}/
    └── page.tsx
        └── {PageComponent}
            ├── {ChildComponent1}
            └── {ChildComponent2}
```

### 4.2 共通コンポーネント

| コンポーネント | 出典 | 用途 |
|--------------|------|------|
| {名前} | {出典} | {用途} |

---

## 5. データフロー

### 5.1 データ受信フロー

```
{データフロー図（テキストベース）}
```

### 5.2 主要データ構造

| データ | 形式 | 格納先 | 説明 |
|--------|------|--------|------|
| {名前} | {形式} | {格納先} | {説明} |

---

## 6. API 設計

### 6.1 IF-{連番}: {インターフェース名}

| 項目 | 内容 |
|------|------|
| エンドポイント | {パス} |
| 呼出元 | {呼出元} |
| 呼出先 | {呼出先} |
| プロトコル | HTTP |
| 同期/非同期 | {同期/非同期} |

#### リクエスト

```json
{
  "field": "value"
}
```

#### レスポンス

```json
{
  "field": "value"
}
```

#### エラー

| コード | 意味 | 対処 |
|--------|------|------|
| {code} | {意味} | {対処} |

---

## 7. 状態管理

### 7.1 方針

| 種別 | 管理方法 | 例 |
|------|---------|-----|
| サーバー状態 | {方法} | {例} |
| UI 状態 | {方法} | {例} |
| フォーム状態 | {方法} | {例} |

---

## 8. 非機能設計方針

### 8.1 性能

| 項目 | 方針 |
|------|------|
| {項目} | {方針} |

### 8.2 可用性

| 項目 | 方針 |
|------|------|
| {項目} | {方針} |

### 8.3 セキュリティ

| 項目 | 方針 |
|------|------|
| {項目} | {方針} |

---

## 9. 前提条件・制約

### 9.1 前提条件

- {前提条件}

### 9.2 制約

- {制約条件}

---

## 10. 未確定事項（Open Questions）

- [ ] Q1: {未確定事項1}
- [ ] Q2: {未確定事項2}

---

## 変更履歴

| 日付 | バージョン | 変更内容 | 作成者 |
|------|-----------|---------|--------|
| YYYY-MM-DD | 1.0 | 初版作成 | @{作成者} |
